# Git ignore for the Garvey deploy repo.
# Only yarmouth-departure.html + its public/ assets + Vercel config are deploy-shaped.

# Large / non-deployable
voyage-3d/
mockups/
poster-studio/
posters/
src/
scripts/
corpus/
node_modules/

# Build output — manifest.json lands in public/ via scripts/build-manifest.mjs
# plus staged copies of archive + poster assets. Source of truth lives in
# /archives/ and /posters/ at the demo/repo roots, not here.
public/archives/
public/posters/

# Not-yet-ready
server.mjs

# Working docs (lives in parent RI repo instead)
README.md
COLLABORATION-BRIEF.md
CORPUS-AUDIT-2026-04-11.md

# OS / env
.DS_Store
.env
.env.local
*.log
.vercel
